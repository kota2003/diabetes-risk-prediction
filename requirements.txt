# ──────────────────────────────────────────────
# requirements.txt
# Updated: Phase 5 — Evaluation & Interpretation
# Environment: diabetes-ml (conda)
# Python: 3.11
# ──────────────────────────────────────────────

# ── Core ──────────────────────────────────────
python>=3.11

# ── Phase 0: Data Collection ──────────────────
pandas>=2.0.0          # ASC fixed-width parsing, CSV I/O, data manipulation
numpy>=1.26.0          # Numerical operations
jupyter>=1.0.0         # Notebook environment
ipykernel>=6.0.0       # Jupyter kernel

# ── Phase 1: Data Understanding ───────────────
matplotlib>=3.7.0      # Distribution plots, heatmaps, bar charts
seaborn>=0.12.0        # Correlation heatmap, styled plots

# ── Phase 2: Data Cleaning ────────────────────
# No new libraries — pathlib is Python stdlib

# ── Phase 3: Feature Engineering ──────────────
scikit-learn==1.4.2       # StandardScaler, train_test_split, metrics
imbalanced-learn==0.14.1  # SMOTE oversampling
statsmodels>=0.14.0       # Variance Inflation Factor (VIF)
scipy>=1.11.0             # Chi-square test (chi2_contingency)
joblib>=1.3.0             # Scaler and model serialisation (save/load .pkl)

# ── Phase 4: Modeling ─────────────────────────
xgboost>=2.0.0         # Gradient boosting model (XGBClassifier)

# ── Phase 5: Evaluation & Interpretation ──────
shap>=0.44.0           # SHAP values for global and individual model explanations
